wp_enqueue_script(
    'my-react-app',                                  // 1. Handle
    plugins_url('/build/static/js/main.js', __FILE__), // 2. URL zum Script
    array('wp-element'),                             // 3. Abhängigkeiten
    '1.0',                                           // 4. Versionsnummer
    true                                             // 5. Am Seitenende einfügen
);

Abhängigkeiten (array('wp-element'))

- WordPress verwendet wp-element, um React und JSX zu unterstützen. Indem du es als Abhängigkeit angibst, stellt WordPress sicher, dass die benötigten JavaScript-Bibliotheken wie React und ReactDOM verfügbar sind, bevor deine Anwendung geladen wird.
- wordpress scannt den Plugin-Ordner automatisch nach Dateien mit einem Plugin-Header (die Kommentare in der [plugin-name].php)

React is a JavaScript library developed by Facebook that allows developers to build reusable UI components and efficiently update them, resulting in fast and responsive web applications. Vite is a fast build tool and development server that is designed to enhance the development experience for modern web applications.

SWC (Speedy Web Compiler), a fast JavaScript/TypeScript compiler written in Rust


defined('ABSPATH') || exit; => to prevent direct access to file


plugin_dir_path(__FILE__)
WordPress-Specific: This function is designed specifically for WordPress plugins and ensures you get a path relative to the plugin directory.
Consistency with WordPress Standards: Using plugin_dir_path() keeps your code consistent with other WordPress functions, especially if it’s being adapted for use in multiple plugins or within environments where WordPress file paths are crucial.
Absolute Path: It returns the absolute path to the file and is commonly used in WordPress plugins to maintain clarity and prevent issues when accessing files in different server setups.